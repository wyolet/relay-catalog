apiVersion: relay.wyolet.dev/v1alpha2
kind: HostBinding
metadata:
    name: custom-on-llmgateway
    displayName: custom via llmgateway
spec:
    model: custom
    host: llmgateway
    adapter: openai
    enabled: true
    snapshots:
        - custom
