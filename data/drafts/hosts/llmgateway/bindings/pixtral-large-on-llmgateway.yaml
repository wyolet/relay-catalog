apiVersion: relay.wyolet.dev/v1alpha2
kind: HostBinding
metadata:
    name: pixtral-large-on-llmgateway
    displayName: pixtral-large via llmgateway
spec:
    model: pixtral-large
    host: llmgateway
    adapter: openai
    enabled: true
    snapshots:
        - pixtral-large-latest
