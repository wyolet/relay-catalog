apiVersion: relay.wyolet.dev/v1alpha2
kind: HostBinding
metadata:
    name: qwen-vl-max-on-llmgateway
    displayName: qwen-vl-max via llmgateway
spec:
    model: qwen-vl-max
    host: llmgateway
    adapter: openai
    enabled: true
    snapshots:
        - qwen-vl-max
