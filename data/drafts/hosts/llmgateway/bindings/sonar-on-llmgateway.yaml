apiVersion: relay.wyolet.dev/v1alpha2
kind: HostBinding
metadata:
    name: sonar-on-llmgateway
    displayName: sonar via llmgateway
spec:
    model: sonar
    host: llmgateway
    adapter: openai
    enabled: true
    snapshots:
        - sonar
