apiVersion: relay.wyolet.dev/v1alpha2
kind: HostBinding
metadata:
    name: auto-on-llmgateway
    displayName: auto via llmgateway
spec:
    model: auto
    host: llmgateway
    adapter: openai
    enabled: true
    snapshots:
        - auto
