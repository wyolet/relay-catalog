apiVersion: relay.wyolet.dev/v1alpha2
kind: HostBinding
metadata:
    name: text-max-on-the-grid-ai
    displayName: text-max via the-grid-ai
spec:
    model: text-max
    host: the-grid-ai
    adapter: openai
    enabled: true
    snapshots:
        - text-max
