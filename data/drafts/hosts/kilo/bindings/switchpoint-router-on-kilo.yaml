apiVersion: relay.wyolet.dev/v1alpha2
kind: HostBinding
metadata:
    name: switchpoint-router-on-kilo
    displayName: switchpoint-router via kilo
spec:
    model: switchpoint-router
    host: kilo
    adapter: openai
    enabled: true
    snapshots:
        - switchpoint-router
