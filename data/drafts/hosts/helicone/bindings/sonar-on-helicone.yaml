apiVersion: relay.wyolet.dev/v1alpha2
kind: HostBinding
metadata:
    name: sonar-on-helicone
    displayName: sonar via helicone
spec:
    model: sonar
    host: helicone
    adapter: openai
    enabled: true
    snapshots:
        - sonar
