apiVersion: relay.wyolet.dev/v1alpha2
kind: Provider
metadata:
    name: upstage
    displayName: Upstage
    description: Upstage — model originator.
spec:
    icon:
        path: /provider/upstage.png
