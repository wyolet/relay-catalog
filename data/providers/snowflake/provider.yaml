apiVersion: relay.wyolet.dev/v1alpha2
kind: Provider
metadata:
    name: snowflake
    displayName: Snowflake
    description: Snowflake — auto-imported, requires hand curation.
    owner:
        kind: system
spec: {}
