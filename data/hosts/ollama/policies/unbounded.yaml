apiVersion: relay.wyolet.dev/v1alpha2
kind: Policy
metadata:
  name: ollama-unbounded
  displayName: Ollama (unbounded)
  description: "Self-hosted Ollama has no provider-imposed upstream limits — saturation is a property of the local GPU. No relay-side cap."
  owner:
    kind: host
    name: ollama
spec: {}
